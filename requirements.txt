# Shared
numpy>=1.24
Pyfhel>=3.4

# Client (CLI only — the desktop GUI has been removed)
datasketch>=1.6
pygeohash>=1.2
requests>=2.31

# Server
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.5

# Packaging (only needed when building binaries)
# pyinstaller>=6.3
