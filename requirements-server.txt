# Server-only dependencies.
#
# Deliberately excludes the desktop client stack (PySide6 / Qt,
# datasketch, pygeohash, requests) so the deployed server image stays
# slim. Keep in sync with the "Shared" + "Server" groups of
# requirements.txt.

# Shared (wire format + BFV params)
numpy>=1.24
Pyfhel>=3.4

# Server
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.5
